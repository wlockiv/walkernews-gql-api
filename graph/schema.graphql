type Link {
    id: ID!
    title: String!
    address: String!
    createdAt: Time
    user: User!
}

type User {
    id: ID!
    username: String!
}

type Query {
    links: [Link!]!
    link(id: ID!): Link!
}

input NewLink {
    title: String!
    address: String!
    userId: String!
}

input RefreshToken {
    token: String!
}

input NewUser {
    username: String!
    password: String!
}

input Login {
    username: String!
    password: String!
}

type Mutation {
    createLink(input: NewLink!): Link!
    createUser(input: NewUser!): User!
    login(input: Login!): String!
}

scalar Time